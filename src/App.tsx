import { Container } from "@mui/material";
import Header from "./components/Header/Header";

const App: React.FC = () => {
  return (
    <Container maxWidth="xl">
      <Header />
    </Container>
  );
};

export default App;
